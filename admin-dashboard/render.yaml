# Admin Dashboard - render.yaml
services:
  - type: web
    name: admin-dashboard
    runtime: static
    plan: free
    rootDir: admin-dashboard
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://chat-backend-main.onrender.com
